// Math = built-in object that provides a collection of properties and methods

let x = 3;
let y = 2;
let z = 1;

//z = Math.round(x); -- Arredonda um número para o inteiro mais próximo.
//z = Math.floor(x); -- Arredonda um número para baixo, para o inteiro mais próximo.
//z = Math.ceil(x); -- Arredonda um número para cima, para o inteiro mais próximo.
//z = Math.trunc(x); -- Remove a parte decimal de um número, apenas retornando a parte inteira.
//z = Math.pow(y, x); -- Retorna a base elevada à potência do expoente.
//z = Math.sqrt(x);
//z = Math.log(x); -- Retorna o logaritmo natural (base e) de um número.
//z = Math.sin(x);
//z = Math.cos(x);
//z = Math.tan(x);
//z = Math.abs(x); -- Retorna o valor absoluto de um número, ou seja, sem o sinal.
//z = Math.sign(x); -- Retorna o sinal de um número, indicando se o número é positivo, negativo ou zero.


//let max = Math.max(x, y, z);
let min = Math.min(x, y, z);

console.log(min);